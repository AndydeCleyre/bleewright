[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "template_utils"
authors = [{name = "Andy Kluger"}]
dynamic = ["version", "description"]
dependencies = ["plumbum", "wheezy.template"]

[project.urls]
Home = "https://github.com/AndydeCleyre/bleewright"
